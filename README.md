Open with **https**://**127.0.0.1**/ (not localhost)

### Build and run project:

#### Directly with nodejs:

    npm i
    npm start

#### with docker

    ./start.sh
    
Use `./start.sh --local` to have a development environment that binds the local directories to the container.
